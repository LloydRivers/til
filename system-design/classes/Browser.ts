export class Browser {
  /*
    Initiates request for a domain.
    
     */
  request() {
    console.log("code is running");
  }
}
// const browser = new Browser()
// function called: browser.request("www.example.com") Simulates user request
// ISPRecursiveResolver receives DNS queries from browser and returns information
// cache hit ? return information :
