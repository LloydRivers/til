class ISPRecursiveResolver {
  /*
    Needs a cache to store previous lookups.
  
    Receives queries from the Browser.
  
    If cache contains the domain, return immediately.
  
    If not, it queries the RootDNSServer, then follows the chain.
  
    Handles the entire resolution process on behalf of the Browser.
     */
}
